import React, { useState } from 'react';

const Inventory = () => {
    const [products, setProducts] = useState([]);
    return (
        <div>
            
        </div>
    );
};

export default Inventory;